CREATE TABLE task_types (id INT IDENTITY NOT NULL, label NVARCHAR(255) NOT NULL, PRIMARY KEY(id));
CREATE TABLE users (id NVARCHAR(255) NOT NULL, username NVARCHAR(255) NOT NULL, password NVARCHAR(255) NOT NULL, salt NVARCHAR(255) NOT NULL, roles NVARCHAR(255) NOT NULL, PRIMARY KEY(id));
CREATE TABLE tasks (id NVARCHAR(255) NOT NULL, user_id NVARCHAR(255) DEFAULT NULL, type_id INT DEFAULT NULL, subject NVARCHAR(255) NOT NULL, dueDate DATETIME2(6) NOT NULL, PRIMARY KEY(id));
CREATE INDEX IDX_50586597A76ED395 ON tasks (user_id);
CREATE INDEX IDX_50586597C54C8C93 ON tasks (type_id);
ALTER TABLE tasks ADD CONSTRAINT FK_50586597A76ED395 FOREIGN KEY (user_id) REFERENCES users(id);
ALTER TABLE tasks ADD CONSTRAINT FK_50586597C54C8C93 FOREIGN KEY (type_id) REFERENCES task_types(id)

